export const metadata = {
  title: "Profile Login | RC Tours & Travels",

  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLoginLayout({ children }) {
  return <>{children}</>;
}
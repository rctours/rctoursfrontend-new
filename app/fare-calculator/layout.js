export const metadata = {
  title: "Fare Calculator | RC Tours & Travels",

  robots: {
    index: false,
    follow: false,
  },
};

export default function FareCalculatorLayout({ children }) {
  return <>{children}</>;
}
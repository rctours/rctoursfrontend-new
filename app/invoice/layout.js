export const metadata = {
  title: "Invoice | RC Tours & Travels",

  robots: {
    index: false,
    follow: false,
  },
};

export default function InvoiceLayout({ children }) {
  return <>{children}</>;
}
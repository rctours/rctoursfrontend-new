export const metadata = {
  title: "Driver Booking | RC Tours & Travels",

  robots: {
    index: false,
    follow: false,
  },
};

export default function DriverStartBookingLayout({ children }) {
  return <>{children}</>;
}
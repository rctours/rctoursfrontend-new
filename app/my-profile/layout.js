export const metadata = {
  title: "My Profile | RC Tours & Travels",

  robots: {
    index: false,
    follow: false,
  },
};

export default function MyProfileLayout({ children }) {
  return <>{children}</>;
}